# Dashboard

The working GUI/API now lives in the flat project root.

Start it from the project root:

```powershell
python -m pip install -r requirements-gui.txt
python run_gui.py
```

Open `http://127.0.0.1:8000`.
